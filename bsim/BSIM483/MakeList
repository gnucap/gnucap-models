

TARGET = bsim483
$(TARGET):

include makedefs

HDRS = bsim4def.h  bsim4ext.h  bsim4itf.h wrapper.h

SPICE_VERSION = SPICE_3f

TESTDIR = ../tests
TESTCASES = b4_ b4bin_

GNUCAP_LOADS = -a mgsim -a ./${TARGET}.so
GNUCAP_INCLUDES = --acs -i ../BSIM481/benchmark_test/modelcard.nmos -i ../BSIM481/benchmark_test/modelcard.pmos \
	--verilog -i ../BSIM483/test_config.parameter --acs
GNUCAP_ARGS = ${GNUCAP_LOADS} ${GNUCAP_INCLUDES}
